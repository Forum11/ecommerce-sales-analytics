/*
    This macro is available via dbt_utils.generate_surrogate_key.
    This file documents the usage pattern for reference.

    Usage in models:
        {{ dbt_utils.generate_surrogate_key(['column_a', 'column_b']) }}

    The dbt_utils package is included in packages.yml.
*/

-- No custom macro needed; using dbt_utils.generate_surrogate_key directly.
